# 📅 2026-04-17 Oracle_DDL-Board
### 🏷️ Topic
데이터 정의어 DDL 학습 및 미니 프로젝트 게시판 만들기

### 📝 What I Learned
 

DML : 데이터 조작 (처리) ==> DML 단위가 ROW  

      SELECT : 데이터 검색  
      INSERT : 데이터 추가
	  			1. 전체 값 추가
	  			INSERT INTO board VALUES(값,값...) => 문자열 ' ' , 날짜 ' '
					 		----- board에 있는 모든 컬럼값을 추가
					  	    ----- DEFAULT 가 무용

				2. 지정된 값 추가 
			    INSERT INTO board(no, name, subject, content, pwd) VALUES (개수만큼 지정)


      UPDATE : 데이터 수정
	  			1. 전체 수정
	  			UPDATE table_name SET
				컬럼 = 값, 컬럼 = 값...

				2. 조건에 맞는 데이터 수정
				UPDATE table_name SET
				컬럼 = 값, 컬럼 = 값...
				WHERE 조건...



      DELETE : 데이터 삭제 ===> CRUD
	  			1. 전체 삭제
				DELETE FROM table_name
				
				2. 조건에 맞는 데이터 삭제
				DELETE FROM table_name
				WHERE 조건


DDL : 데이터 정의어 ==> DDL 단위가 COLUMN

      CREATE : 생성 
  			     TABLE = 데이터 저장하는 공간***
		        	SEQUENCE = 자동 증가 번호 (게시판 => 번호) 
			        VIEW = 가상 테이블 => SELECT 저장
		        	INDEX = 검색 최적화 / 정렬
		        	FUNCTION / PROCEDURE / TRIGGER : 5장
	        		==> AutoCommit
      ALTER : 추가 / 수정 / 삭제
			        ADD, MODIFY, DROP
      DROP : 전체 삭제
      RENAME : 테이블 이름 변경 = 리팩토링
      TRUNCATE : 삭제 => 테이블은 유지
								=> 데이터만 삭제 => 잘라내기

오라클에서 지원하는 데이터 : 
							1) 문자형 : 문자, 문자열 저장 
										CHAR
										VARCHAR2
										CLOB

							2) 숫자형 : 정수, 실수
										NUMBER 
										
							3) 날짜형 : DATE => SYSDATE 
										DATE, TIMESTAMP

							4) 기타 => 이미지, 동영상...
										BFILE / BLOB

 데이터 유지 = 제약조건 :
 
						정형화된 데이터  : 규격에 맞게 저장 => 바로 사용이 가능
										구분이  잘되어 있다 => 데이터베이스
										
						반정형화된 데이터 : 구분이 된 데이터 => XML, HTML, JSON => 크롤링
						
						비정형화된 데이터 : 규칙도 없고 구분도 없는 데이터 : 트위터, facebook
					   ---------------------> 분석 ==> 필요한 데이터만 정형화된 데이터로 변경




### 💡 Note
미니 프로젝트 게시판 만들기를 진행했다. 

일단 아직 웹을 배우지 않아 윈도우창으로 실습을 했기에 가장 중요했던건 데이터베이스와 자바를 연동하는 것이었다.

BoardVO는 데이터베이스에서 데이터를 불러왔을 때  DB에 저장된 칼럼과 일치하는 변수를 만들어 저장하는 변수창고를 만드는 것이고

BoardDAO는 DB와 JAVA를 연결하는 역할을 하는 클래스로 그 흐름을 파악하는게 중요하다.

		1. DB 주소 등록(Connection) - 2. 드라이버 등록 - 3. 연결 - 4. 닫기 - 5. 싱글턴
		
으로 이루어 지는 공통사항이 있고 이후는

기능을 구현하는데 일단 어떤 기능을 어떤 순서로 구현할 것인가 구상을 해두고 들어가는 것이 좋다

		1. 목록출력(페이징) -> 1.1 총페이지 구하기 -> 2.상세보기(WHERE) -> 3. 글쓰기(INSERT) -> 4.수정(UPDATE) -> 5. 삭제(DELETE) -> 6. 찾기(LIKE)
