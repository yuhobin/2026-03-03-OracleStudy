# 📅 2026-04-30 MiniProject_day7
### 🏷️ Topic
도서관리 프로젝트 마이페이지 생성

### 📝 What I Learned
프로젝트에서 내가 담당한 마이페이지를 만들었다 

진행 순서 

          먼저 메커니즘 파악
          
          공용DB에 책 정보 수집하여 추가
          
          마이페이지 프레임 및 버튼 만들기(윈도우 창으로 생성)

          
          
          

DAO 제작 공통사항 

                  1. 멤버 변수 및 DBUtil 객체 생성
                  
                  2. 싱글턴 패턴
                  
                  3. 오라클 연결 (DBUtil 활용)
                  
                  4. 오라클 해제 (DBUtil 활용)

마이페이지 기능 구현

                대출중인 도서목록 구현  
               String sql="SELECT orderid, bookname, author, isbn, "
	                   + "TO_CHAR(ordate,'YYYY-MM-DD'), TO_CHAR(duedate,'YYYY-MM-DD'), "
	                   + "CASE WHEN TRUNC(SYSDATE) > TRUNC(duedate) THEN '연체중' ELSE '대출중' END AS status, poster "
	                   + "FROM orders a JOIN book b "
	                   + "ON a.bno=b.bno "
	                   + "AND a.memid=? "
	                   + "ORDER BY orderid";

                더블 클릭시 반납기능 구현
               String sql="DELETE FROM orders "
		       +"WHERE orderid="+orderid;

### 💡 Note
항상 어떤 기능을 구현할건지 정리를 하고 주석처리로 순서를 정한 뒤 코드를 짜는게 큰 도움이 된다

이번 프로젝트에선 sql 쿼리문을 잘 만들어서 작성했을때 데이터가 문제없이 연결되는것을 중점으로 진행했다

처음에 에러도 몇번나고 DB연결이 안될때도 있었는데 사소한 오타나 sql의 문법적인 부분에서 오류가 있었다

이를 바로 잡아나가는 과정이 쿼리문을 작성하는 능력에 큰 도움이 됐다
